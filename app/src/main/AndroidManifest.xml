<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.myapplication">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MyPostActivity"></activity>
        <activity android:name=".EditProfileActivity" />
        <activity android:name=".VerifyActivity" />
        <activity android:name=".VerfiyActivity" />
        <activity android:name=".SearchTutorActivity" />
        <activity android:name=".SearchTuitionActivity" />
        <activity android:name=".ContactActivity" />
        <activity android:name=".HelpActivity" />
        <activity android:name=".FindTutorActivity" />
        <activity android:name=".FindTuitionActivity" />
        <activity android:name=".PostActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".SignInActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>